# 2026-09-18 迭代记录：支付宝沙箱支付接入

> 承接《改良计划.md》（2026-09-06）与 2026-09-13 迭代记录第六节遗留事项
> 「支付宝沙箱密钥待配置」——本轮将支付渠道从 Mock 演示升级为支付宝沙箱真实接入。
> 分支：`feat/alipay-sandbox-pay`（PR 合入 `main`，不直接推 main）。

## 一、背景

原支付模块只有 Mock 渠道：支付页展示「模拟支付成功 / 模拟支付失败」按钮，
由 `/api/pay/mock/callback` 触发入账。`AlipayGatewayClient` 接口注释里标着
`AlipaySandboxClient（TODO：接入支付宝开放平台沙箱）`，即渠道抽象已就位但真实实现缺失。
本轮补齐真实实现，并保持 Mock 可用（本地无密钥也能跑通全流程）。

## 二、渠道抽象重构

| 项 | 改动 |
|---|---|
| 返回值 | `createPay` 由返回 `String`（payNo）改为返回 `PayCreateResult`（payNo / orderNo / 应付金额 / **支付跳转 URL** / 是否真实渠道） |
| 退款 | `refundPay` 增加 `reason` 参数；失败改为 **抛 `BizException`**，不再依赖 `MOCK-REFUND-` 字符串前缀判断成功 |
| 新增能力 | `closePay` 关单（订单取消后防止继续付款）、`channelCode()`、`isRealChannel()` |
| 新增方法 | `PayInfoMapper.markClosed`（`WHERE status = 0` 条件更新，关闭支付单） |

## 三、真实沙箱实现

新增 `AlipaySandboxClient`（`com.alipay.sdk:alipay-sdk-java:4.38.200.ALL`，父 pom 统一管版本）：

| 能力 | 支付宝接口 |
|---|---|
| 电脑网站支付 | `alipay.trade.page.pay` —— 生成收银台跳转 URL（从返回表单的 `action` 提取） |
| 退款 | `alipay.trade.refund` —— `out_request_no` 作渠道侧幂等号 |
| 关单 | `alipay.trade.close` —— 失败不阻断业务，仅告警日志 |
| 回调验签 | `AlipaySignature.rsaCheckV1`（RSA2） |

装配策略（`GatewayConfiguration`，沙箱与 Mock **互斥**）：

| `mall.alipay.enabled` | 三密钥 | 结果 |
|---|---|---|
| `false`（默认） | 任意 | `MockAlipayClient`，本地演示闭环 |
| `true` | 齐全 | `AlipaySandboxClient`，真实沙箱收款 |
| `true` | 缺失 | **启动直接失败**（`IllegalStateException`，报错指明缺哪个变量） |

> 最后一条是刻意设计：宁可启动报错，也不要出现「以为在收真钱、其实用的 Mock」。

## 四、回调安全（本轮重点）

`POST /api/pay/alipay/notify` 处理顺序**不可调换**：

1. **RSA2 验签** —— 证明通知确实来自支付宝，防伪造回调
2. **交易状态校验** —— 仅 `TRADE_SUCCESS` / `TRADE_FINISHED` 才入账
3. **Redisson 分布式锁** `pay:notify:{payNo}` —— 并发回调只进一个
4. **金额核对** —— 渠道实付必须等于支付单金额，不一致拒绝入账
5. **状态机条件更新** —— `UPDATE pay_info SET status=1 WHERE id=? AND status=0`，重复回调 0 行即幂等返回
6. **唯一索引兜底** —— `pay_info.uk_out_trade_no`

任何一步不通过均返回纯文本 `failure`（应答体不能包成统一 JSON，否则支付宝会一直重试）。

**网关白名单**：`/api/pay/alipay/notify` 加入 `AuthGlobalFilter.GUEST_PATHS` ——
支付宝服务器没有也不可能有 JWT，安全性由下游验签 + 金额核对保证，不靠鉴权。

## 五、前端

| 改动 | 说明 |
|---|---|
| 支付页识别渠道 | `realChannel=true` 显示应付金额 + 「前往支付宝付款」（新窗口打开收银台）并轮询支付状态；`false` 保留原「模拟支付」按钮 |
| 新增落地页 | `/pay/result`（`PayResult.vue`）处理支付宝**同步跳转**；该页轮询后端状态而非依据 URL 参数判成功 |
| 路由顺序 | `pay/result` 必须置于 `pay/:orderNo` 之前，否则 "result" 会被当成订单号 |
| 新增接口 | `GET /api/pay/info/{orderNo}` 查支付凭证 |

> 同步跳转只代表「用户从支付宝回来了」，**不代表已入账**；入账以异步通知为准。

## 六、验证

| 检查 | 结果 |
|---|---|
| 编译 | 后端 10 模块 `mvn install` BUILD SUCCESS；前端 `vue-tsc` 类型检查 + `vite build` 通过 |
| 单测 | **36 项全部通过**（`PayServiceTest` 24 + `AlipaySandboxClientTest` 9 + `AlipayPropertiesBindingTest` 3） |
| 验签单测 | 用本地 RSA2048 密钥对按支付宝规则签名：合法签名通过、**篡改金额失败**、缺 `sign` 失败、公钥配错失败、**PEM 格式密钥归一化后通过** |
| 配置绑定单测 | 用 Spring 的 YAML 加载器读真实 `application.yml` 做绑定：默认走 Mock 且默认值完整、`MALL_ALIPAY_*` 环境变量覆盖可解析、`notifyBase` 末尾斜杠归一化、服务端口与库名约定正确 |
| 端到端（Mock） | 下单 → 创建支付 → 回调 → 入库：订单 `status=1`、支付单 `status=1`、`trade_no` 正确写入，金额 3×3299=9897 核对通过 |
| 幂等 | 重复 `create` 复用同一支付单；重复回调不重复入账 |
| 回调入口 | Mock 模式下 POST 通知返回 `HTTP 200 failure`（白名单生效 + 安全拒绝） |
| 安全兜底 | `enabled=true` 且密钥缺失时服务 exit=1，报错信息明确 |

### 六之补充：开发期发现并修复的缺陷

| 缺陷 | 根因 | 修复 |
|---|---|---|
| **PEM 格式密钥导致回调验签静默失败** | 支付宝密钥生成工具导出的是 PEM（多行 + `BEGIN/END` 头尾），而 SDK 的 `AlipaySignature` 只接受单行 Base64；且 `verifyNotify` 读的是 `props` 上的原始值，构造函数里做的归一化只赋给了局部变量，**等于没生效** | 归一化结果存入实例字段供验签使用；`normalizeKey` 剥掉头尾与全部空白字符，PEM 与单行 Base64 均可直接粘贴。补 2 项回归测试锁定 |
| Maven 重命名失败导致 `mall-gateway` jar 变 0 字节 | 服务进程仍在运行、Windows 锁住 jar，`spring-boot:repackage` 无法 `rename` | 先停服再构建；已清理 target 重建（见「排错」提示：构建前务必停掉服务） |


## 七、遗留事项

- **沙箱真实收款未验证**：需要沙箱 APPID / 密钥 + **公网可达的异步通知地址**。
  本机开发需内网穿透（ngrok / cpolar）把网关 8090 暴露出去，步骤见
  `docs/支付宝沙箱接入指南.md` 第 4 节。代码路径已按官方文档实现并有单测覆盖。
- `/api/pay/info/{orderNo}` 目前不返回支付跳转 URL（返回 `null`），
  待支付场景下前端需再调一次 `/create` 取凭证；如需一步到位可让该接口重新生成 URL。
- 退款走同步返回，生产环境应改为「渠道异步退款结果 + 重试补偿」。
- 支付成功/退款仍为**同库直写** order/merchant（单库演示约定），
  生产演进应改发 MQ 事件由各服务自行消费（`PayService` 注释已标明 TODO）。

## 八、配套文档

- 新增 `docs/支付宝沙箱接入指南.md`：密钥获取、配置方式（环境变量 / `application-local.yml`）、
  异步通知公网地址要求、演示步骤、联调排错表、生产环境差异。
- `.gitignore` 增加 `application-local.yml` 等本地敏感配置忽略规则（密钥不入库）。
